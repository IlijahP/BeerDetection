[project]
name = "beerdetection"
version = "0.1.0"
description = "Live beer detection using YOLO and webcam"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "ultralytics",
    "opencv-python"
]